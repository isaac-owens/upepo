// Webcam feed
export const webcam = document.getElementById("webcam");

// Drawn webcam feed
export const canvasSource = document.getElementById("canvas-source");
export const contextSource = canvasSource.getContext("2d");

// Blended frames
export const canvasBlended = document.getElementById("canvas-blended");
export const contextBlended = canvasBlended.getContext("2d");
